---
- name: "update apt"
  become: true
  apt: 
    update_cache: yes

- name: "upgrade apt packages."
  become: true
  apt: 
    upgrade: yes

- name: "install dependencies."
  become: true
  apt: 
    name: ["nodejs", "npm", "pm2"]
    state: latest
    update_cache: yes
